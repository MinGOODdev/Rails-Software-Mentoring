<div class="row">
  <div class="col"></div>

  <div class="col-6">
    <div class="table-responsive text-center">
      <h3>현재 옵션 현황</h3>
      <table class="table table-bordered">
        <thead>
        <tr>
          <th>항목</th>
          <th>옵션</th>
        </tr>
        </thead>
        <tbody>
        <tr>
          <td>멘토 신청</td>
          <td>
            <% if @adminOption.mentor_apply_active == 1 %>
              활성화
            <% elsif @adminOption.mentor_apply_active == 0 %>
              비활성화
            <% end %>
          </td>
        </tr>
        <tr>
          <td>멘티 신청</td>
          <td>
            <% if @adminOption.mentee_apply_active == 1 %>
              활성화
            <% elsif @adminOption.mentee_apply_active == 0 %>
              비활성화
            <% end %>
          </td>
        </tr>
        <tr>
          <td>멘토방 최대 인원</td>
          <td><%= @adminOption.room_member_max_num %> 명</td>
        </tr>
        </tbody>
      </table>
    </div>

    <!-- 활성화 & 비활성화 버튼 -->
    <div>
      <div class="float-right">
        <a href="/admin/menteeOption" class="btn btn-warning">
          <% if @adminOption.mentee_apply_active == 1 %>
            멘티 신청 비활성화
          <% elsif @adminOption.mentee_apply_active == 0 %>
            멘티 신청 활성화
          <% end %>
        </a>
      </div>

      <div class="float-left">
        <a href="/admin/mentorOption" class="btn btn-warning">
          <% if @adminOption.mentor_apply_active == 1 %>
            멘토 신청 비활성화
          <% elsif @adminOption.mentor_apply_active == 0 %>
            멘토 신청 활성화
          <% end %>
        </a>
      </div>
    </div>
    <!-- ./ 활성화 & 비활성화 버튼 -->
  </div>

  <div class="col"></div>
</div>

<hr/>

<div class="text-center div-margin-bottom div-margin-top">
  <div class="row">
    <div class="col"></div>

    <div class="col-10">
      <h3>멘토방 최대 인원 설정</h3>

      <form action="/admin/roomOption" class="text-center" method="post">
        <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
        <div class="form-group" style="width: 15%; margin-left: auto; margin-right: auto;">
          <input class="form-control text-center" id="room_member_max_num" name="room_member_max_num" value="<%= @adminOption.room_member_max_num %>" placeholder="숫자만 입력하세요.">
        </div>
        <button class="btn btn-success" type="submit">인원 설정</button>
      </form>
    </div>

    <div class="col"></div>
  </div>
</div>
